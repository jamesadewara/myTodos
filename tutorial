// ElevatedButton(
//             onPressed: () {
//               // Restart the app when the button is pressed
//               window.scheduleFrame(); // Call scheduleFrame to restart the app
//             },